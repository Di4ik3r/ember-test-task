Projet structure: \
	|--->data (json files) \
	|--->src (source code) \
	|-------> node file (index) for data transfer: .json -> postgresql \
	|--------------> back project (node/express) \
	|--------------> front project (ember)
